{
  "name": "ryuta-suzuki-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Static portfolio for Ryuta Suzuki with live Unsplash gallery.",
  "scripts": {
    "dev": "live-server --open=./index.html --port=4173 --host=0.0.0.0",
    "start": "live-server --port=8080 --host=0.0.0.0 --no-browser"
  },
  "dependencies": {
    "live-server": "^1.2.2"
  }
}
